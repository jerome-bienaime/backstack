{
	"devDependencies": {
		"cypress": "^13.7.1"
	},
	"dependencies": {
		"start-server-and-test": "^2.0.3"
	},
    "scripts": {
        "serve": "ENV=test ./web",
        "e2e": "cypress run",
        "ci": "start-server-and-test serve http://localhost:8080 e2e"
    }
}
